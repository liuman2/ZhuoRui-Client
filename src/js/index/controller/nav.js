

module.exports = function (
  $scope,
  $rootScope,
  $state
) {

  // menu active
  // $scope.menu = null;
  // $rootScope.$on('$stateChangeSuccess',
  //   function(event, toState, toParams, fromState, fromParams){
  //     $scope.menu = toState.name.split('.')[0];
  // });

};
